name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # dependencies
  - numpy
  - matplotlib
    # Pip-only installs
  #- pip:
  #  - codecov
  

